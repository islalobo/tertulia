/**
 * Implementation of Drupal behavior.
 */
(function($) {
  Drupal.behaviors.carousel = {
    attach: function (context, settings) {
      /**
       * Insert code here
       */
    }
  };
})(jQuery);
